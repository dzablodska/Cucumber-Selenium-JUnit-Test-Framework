package com.nat.facebook.qa.element;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class SectionElement extends PageElement {
    public SectionElement(WebDriver driver, By by) { super(driver, by); }
}
